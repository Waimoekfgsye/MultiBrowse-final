const fs = require('fs'); const path = require('path'); const publicDir = path.join(__dirname, '..', 'public'); const buildDir = path.join(__dirname, '..', 'build'); if (!fs.existsSync(publicDir)) fs.mkdirSync(publicDir, { recursive: true }); if (!fs.existsSync(buildDir)) fs.mkdirSync(buildDir, { recursive: true }); // Skip if custom icon.png already exists if (fs.existsSync(path.join(publicDir, 'icon.png'))) { console.log('Custom icon.png found — skipping SVG generation.'); process.exit(0); } // Fallback: generate SVG icon const svgIcon = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 256 256"><rect width="256" height="256" rx="48" fill="#0a0a0f"/><path d="M128 48l56 32v64l-56 32-56-32V80z" fill="none" stroke="#e8d44d" stroke-width="8"/><circle cx="128" cy="128" r="20" fill="#e8d44d"/></svg>`; fs.writeFileSync(path.join(publicDir, 'icon.svg'), svgIcon); console.log('SVG icon created (no custom PNG found).');
